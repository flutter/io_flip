name: api_client
description: Client to access the api
version: 0.1.0+1
publish_to: none

environment:
  sdk: ">=2.19.0 <3.0.0"

dev_dependencies:
  mocktail: ^0.3.0
  test: ^1.19.2
  very_good_analysis: ^4.0.0

dependencies:
  game_domain:
    path: ../../api/packages/game_domain
  http: ^0.13.5
