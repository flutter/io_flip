name: top_dash_ui

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  pull_request:
    paths:
      - "packages/top_dash_ui/**"
      - ".github/workflows/top_dash_ui.yaml"
    branches:
      - main

jobs:
  build:
    uses: VeryGoodOpenSource/very_good_workflows/.github/workflows/flutter_package.yml@v1.12.0
    with:
      flutter_channel: beta
      flutter_version: 3.10.0-1.1.pre
      working_directory: packages/top_dash_ui
